- 목표: 서울 내 코스 추천 웹 서비스
- 특징: 옵션에 따른 코스 추천 기능 제공
    - 날씨 반영 
        - 덥거나 춥거나 비 예보가 있는 경우, 실내 코스 추천
    - 총 이동 시간 설정
    - 가능한 교통수단 선택: 버스, 지하철, 도보(코스 최대 도보 거리 설정/예: 500m), 택시(코스 간 교통수단 경로가 없는 경우.)
    - 코스 추천받을 구체적인 장소 선택

- 사용 기술 및 api:
    - 비짓서울 api
    - 서울 열린 데이터 api
    - 기상청 단기예보 조회서비스 api
    - 서울시 대중교통환승경로 API 
    - 서울교통공사 최단경로 API

---

# 상세 서비스 스펙

## 1. 서비스 범위와 용어

- 사용자가 선택한 서울 내 동네(예: 성수) 안에서 카페·식당·놀거리를 묶은 방문 코스를 추천한다. 여러 동네를 순회하는 일정이 아니다. 이 문서에서 '지역'과 '동네'는 같은 뜻으로 쓴다.
- 처음 설정한 취향을 이후 추천에 재사용하고, 선택 지역과 날씨에 맞춰 장소를 구성한다.
- 위 원문은 최초 작성 내용을 보존한 것이며, 구체적인 동작은 아래 상세 스펙을 기준으로 한다.
- 코스는 순서가 있는 방문 장소들의 목록이며, 이동 구간은 연속한 두 장소 사이를 뜻한다.
- **추천 지역의 경계는 행정동 단위**로 정의한다. 예를 들어 '성수'는 성수1가1동·성수1가2동·성수2가1동·성수2가3동의 합집합이다. 지역별 행정동 목록은 서비스가 관리하는 설정 데이터로 두며, 장소의 소속 지역은 주소 또는 좌표를 행정동에 대응시켜 판정한다.
- **제공 지역은 서울 25개 자치구 전역의 동네**이다. 서울의 모든 행정동은 정확히 하나의 동네에 속하며(빠짐없이, 겹침 없이), 동네 목록은 '부록 A. 서울 전역 동네 목록'을 기준으로 한다. 동네 이름은 행정동명이 아니라 사람들이 부르는 생활권 이름(성수, 홍대, 샤로수길 등)을 우선하고, 대표 이름이 없는 곳은 행정동 묶음의 이름(면목, 쌍문 등)을 쓴다.
- 동네의 행정동 구성은 데이터 수집 시 행정안전부 최신 행정동 코드로 확정하며, 행정동 통폐합·개칭이 있으면 설정 데이터만 갱신한다. 동네 추가·분할·병합도 설정 데이터 변경만으로 가능해야 한다.
- 서비스 초기 검수 대상은 **성수·서촌(통의)·익선·홍대·연남 5곳**이다. 이 5곳은 장소 데이터를 사람이 검수하는 '우선 검수 지역'이며, 나머지 동네는 자동 수집·자동 분류 결과로 제공한다.
- 최대 도보 거리는 **장소 간 전체 도보 이동 구간에 적용하는 구간별 기준**이며, 사용자가 설정할 수 있는 상한은 1km(1,000m)이다. 전체 코스의 누적 도보 거리 제한을 뜻하지 않는다. **기본값은 500m**이다.
- 정류장·역 접근 및 환승에 필요한 도보는 최대 도보 거리 제한의 대상이 아니다. 자세한 내용은 3장 '이동수단 결정'을 따른다.
- 공공 API를 우선 사용한다. 문서상 제공 기능과 실제 호출로 검증한 기능을 구분한다.
- 택시 없이 완성되는 코스를 우선 추천한다. 필요할 때 택시 대안을 추가하며, 호출·예약 기능은 포함하지 않는다.

## 2. 사용자 입력

### 2.1 초기 취향

- 첫 이용 시 온보딩에서 설정하고 이후 모든 추천에 재사용한다. 온보딩은 건너뛸 수 있으며, 건너뛴 사용자는 취향 없이 전체 허용 상태로 추천받는다. 설정 화면에서 언제든 입력·수정할 수 있다.
- 취향 항목은 다음 네 가지이다. 음식 종류·놀거리 유형·분위기는 복수 선택, 실내·실외 선호는 단일 선택이다.

| 항목 | 선택지 | 추천에 쓰이는 방식 |
| --- | --- | --- |
| 음식 종류 | 한식·양식·일식·중식·아시안·카페 디저트·기타 | 식당 후보 필터. 선택한 종류에 해당하는 식당만 후보로 둔다. |
| 놀거리 유형 | 전시·체험·쇼핑·공원·공연·기타 | 놀거리 후보 필터. 선택한 유형에 해당하는 장소만 후보로 둔다. |
| 분위기 | 조용함·활기참·감성·힙플 | 정렬 가중치로만 사용한다. 필터로 쓰지 않는다. |
| 실내·실외 선호 | 실내·실외·상관없음 | 정렬 가중치로만 사용한다. **날씨 판단이 실내 우선 모드이면 이 선호는 무시하고 날씨를 따른다.** 자세한 내용은 3장 '날씨와 실내 장소'를 따른다. |

- 음식 종류와 놀거리 유형은 아무것도 선택하지 않으면 전체를 허용한 것으로 본다. 실내·실외 선호의 기본값은 '상관없음'이다.
- 분위기 태그는 장소 설명문에서 키워드 규칙으로 추출한다. 태그가 없는 장소는 '분위기 미확인'으로 두고 후보에서 배제하지 않으며, 분위기 가중치 계산에서만 0으로 처리한다. 키워드 규칙 표는 구현 시 별도 문서로 관리한다.

### 2.2 취향 저장 방식

- **디바이스 내 저장만** 채택한다. 로그인·회원가입·계정 동기화 기능은 제공하지 않는다.
- 취향은 브라우저 저장소(localStorage)에 저장한다. 서버에는 취향을 저장하지 않으며, 사용자를 식별하는 정보도 서버로 보내지 않는다.
- 브라우저나 기기를 바꾸면 취향은 이어지지 않는다. 브라우저 데이터가 삭제되면 취향도 삭제되며, 이 경우 다음 접속 시 온보딩을 다시 띄운다.
- 설정 페이지에 '취향 초기화' 버튼을 두어 저장된 취향을 삭제할 수 있다.

### 2.3 코스 요청 입력

| 항목 | 내용 | 기본값 |
| --- | --- | --- |
| 추천 지역 | 서울 전역 동네 목록(부록 A)에서 하나를 선택한다. 자치구를 고른 뒤 그 안의 동네를 고르는 2단계 선택이며, 동네 이름·행정동 이름으로 검색해 바로 고를 수도 있다. | 없음(필수) |
| 방문 날짜·시간 | 코스를 시작하는 날짜와 시각. 날씨 조회와 각 장소 예상 도착 시각 계산의 기준이다. | 현재 시각 |
| 코스 구성 | 기본은 카페 1·식당 1·놀거리 1의 3곳이다. 사용자가 카테고리별로 개수를 추가·제거할 수 있다. **총 장소 수는 최소 2곳, 최대 5곳**이며 같은 카테고리 중복(예: 카페 2)을 허용한다. | 카페 1·식당 1·놀거리 1 |
| 총 이동시간 상한 | 관광·식사 등 체류시간을 제외한 구간별 이동시간의 합. 분 단위. | 60분 |
| 허용 교통수단 | 버스·지하철·도보·택시 네 가지 체크박스. 버스·지하철·도보는 우선 추천 대상이다. **택시는 대안 표시 전용**이며 우선 추천 경로에는 쓰지 않는다. | 버스·지하철·도보 켜짐, 택시 꺼짐 |
| 구간별 최대 도보 거리 | m 단위. 최대 1,000m. 도보를 허용한 경우에만 입력한다. | 500m |

- **출발 위치는 입력받지 않는다.** 코스는 선택 지역 안에서만 구성되므로 첫 방문 장소가 출발점이다. 사용자의 집·역에서 첫 장소까지의 이동은 코스에 포함하지 않는다.
- 날씨와 경로 계산에 사용한 기준 시각을 결과에 표시한다. 날씨의 기준 위치는 선택 지역의 대표 좌표(행정동 집합의 중심)이다.
- 동네 목록은 자치구별로 묶어 보여준다. 검색은 동네 표시명과 소속 행정동명 모두에 부분 일치로 동작한다(예: '서교' 입력 시 홍대).
- 장소 데이터 수집 후 카테고리별 후보 수를 동네마다 집계한다. 카페·식당·놀거리 중 하나라도 후보가 0개인 동네는 목록에서 회색으로 표시하고 선택 시 '이 동네는 장소 데이터가 부족합니다'를 안내하며, 부족한 카테고리를 함께 표시한다. 회색 동네는 선택할 수 없다.
- 시간·거리 입력은 양수여야 하며, 도보 거리 입력은 1,000m 이하여야 한다. 허용 교통수단은 택시를 제외하고 하나 이상 선택해야 한다. 택시만 선택한 요청은 거부한다.
- 방문 날짜·시간은 현재 시각 이후여야 하며, 기상청 단기예보 제공 범위(발표 시각 기준 최대 3일)를 넘으면 입력은 허용하되 '날씨 미반영'으로 처리한다.

## 3. 추천 처리

1. 선택 지역의 행정동에 속하는 장소 중 초기 취향(음식 종류·놀거리 유형)에 맞는 카페·식당·놀거리 후보를 조회한다. '다시 추천'으로 재요청한 경우 이번 세션에서 이미 보여준 장소는 후보에서 제외한다. 취향에 맞는 후보가 요청한 개수에 못 미치는 카테고리는 '취향 완화' 규칙을 따른다.
2. 방문 시점에 해당하는 날씨를 확인하고, 덥거나 춥거나 비 예보가 있으면 실내 장소를 우선 추천한다.
3. 요청한 코스 구성(카테고리별 개수)에 맞게 장소를 고르고, 택시 없이 이동할 수 있는 방문 순서를 우선 구성한다. 각 이동 구간의 허용 교통수단과 예상 이동시간을 확인한다.
4. 각 장소의 예상 도착 시각을 계산하고, 그 시각에 영업 중이 아닌 장소는 제외한다. 자세한 내용은 '운영시간과 예상 도착 시각'을 따른다.
5. 구간별 최대 도보 거리와 총 이동시간 상한을 적용한다.
6. 분위기 취향과의 일치도, 그리고 실내 우선 모드가 아닐 때의 실내·실외 선호 일치도를 정렬 가중치로 반영한다.
7. 방문 순서, 구간별 이동방법, 거리·시간 및 미확인 정보를 함께 제공한다.

### 취향 완화

- 어떤 카테고리에서 음식 종류·놀거리 유형 필터를 통과한 후보가 요청 개수보다 적으면, **그 카테고리에 한해 필터를 해제**하고 같은 카테고리의 다른 장소로 부족한 개수를 채운다.
- 필터 해제로 채운 장소에는 결과에 '취향 외' 표시를 붙인다. 필터를 통과한 장소를 먼저 배치하고 '취향 외' 장소는 남은 자리에만 쓴다.
- 완화 대상은 취향 필터만이다. 총 이동시간 상한, 구간별 최대 도보 거리, 허용 교통수단, 운영시간 조건은 완화하지 않는다. 이 조건들 때문에 코스를 만들 수 없으면 5장 '로딩과 오류 처리'의 '조건에 맞는 코스 없음' 안내를 따른다.
- 완화 후에도 해당 카테고리의 장소 자체가 부족하면 '조건에 맞는 코스 없음'으로 처리한다.

### 운영시간과 예상 도착 시각

- 첫 장소의 도착 시각은 방문 시각이다. 이후 장소의 도착 시각은 직전 장소의 도착 시각 + 직전 장소의 기본 체류시간 + 이동 구간의 예상 이동시간이다.
- 카테고리별 기본 체류시간은 **카페 60분, 식당 60분, 놀거리 90분**이다. 사용자 입력 항목이 아니다.
- 예상 도착 시각에 영업 중이 아닌 장소는 후보에서 제외한다. 마지막 주문 시간 등은 고려하지 않는다.
- 운영시간 정보가 없는 장소는 후보에 포함하되 결과에 '운영시간 미확인'을 표시한다.
- 이동시간이 미확인인 구간 뒤의 도착 시각은 확정하지 않으며, 그 장소의 운영시간 판단은 '도착 시각 미확인'으로 표시하고 제외하지 않는다.

### 날씨와 실내 장소

- 날씨 판단은 기상청 단기예보의 **방문 시각이 속한 1시간 예보**를 사용한다.
- **덥다: 기온(TMP) 30°C 이상. 춥다: 기온(TMP) 0°C 이하. 비 예보: 강수확률(POP) 60% 이상.** 세 조건 중 하나라도 해당하면 실내 우선 모드로 동작한다.
- 실내 우선 모드에서는 각 카테고리에서 실내 장소를 먼저 고르고, 실내 후보가 부족할 때만 복합·실외 장소를 채운다. 그렇게 채운 장소에는 '실내 아님' 표시를 붙인다.
- **실내 우선 모드는 사용자의 실내·실외 선호보다 우선한다.** 실외 선호 사용자에게 실내 우선 모드가 적용되면 결과에 '비 예보(또는 고온·저온)로 실내 우선' 이유를 표시한다. 실내 우선 모드가 아닐 때만 실내·실외 선호를 정렬 가중치로 반영한다.
- 장소는 실내·실외·복합·미확인으로 구분한다. API가 구분 정보를 제공하지 않으면 두 단계로 보완한다.
    1. **카테고리 기반 자동 추정**: 카페·식당·전시·공연은 실내, 공원은 실외, 쇼핑·체험·기타는 미확인으로 둔다.
    2. **수동 보정**: 우선 검수 지역(성수·서촌·익선·홍대·연남)의 장소는 수집 후 사람이 검수해 저장소 내 분류 파일로 덮어쓴다. 수동 보정값이 자동 추정값보다 우선한다. 그 외 동네는 자동 추정값으로 제공하며, 검수한 동네는 순차적으로 분류 파일에 추가한다.
- 복합·미확인 장소를 실내로 단정하지 않는다.
- 날씨 조회 실패 또는 예보 범위 밖인 경우에는 '날씨 미반영'을 표시하고 실내 우선 모드를 적용하지 않는다.

### 이동수단 결정

- 도보가 허용되고 보행거리가 설정값 이하이면 도보를 후보에 포함한다. 보행거리는 실제 보행거리를 우선 쓰고, 없으면 '거리·시간의 정확성'의 추정 보행거리를 쓴다.
- 도보 거리를 초과하거나 도보가 허용되지 않으면 선택한 버스·지하철 수단으로 경로를 조회한다.
- 대중교통 경로에 사용자가 허용하지 않은 **승차 수단**이 포함되면 해당 경로를 제외한다.
- **정류장·역 접근 및 환승 도보는 대중교통 이용의 일부로 본다.** 최대 도보 거리 제한을 적용하지 않으며, 사용자가 도보를 허용하지 않았더라도 허용한다. 거리와 시간은 결과에 별도로 표시한다. '도보' 선택은 장소 간 전체를 걸어서 이동하는 구간만을 뜻한다.
- 택시 없는 대체 코스를 우선 찾는다. 그래도 허용 조건에 맞는 경로가 없고 사용자가 택시를 허용했으면 해당 구간을 '택시 이용 검토 · 소요시간 미확인'으로 표시한다.
- 택시가 허용되지 않으면 해당 구간을 이동 가능한 것으로 처리하지 않고, 다른 코스를 찾거나 조건에 맞는 코스가 없음을 안내한다.
- API 오류·시간 초과는 '경로 조회 실패'로 표시한다. 정상 조회 결과의 '경로 없음'과 구분하며, 오류만으로 택시 대안으로 전환하지 않는다.

### 거리·시간의 정확성

- 직선거리는 실제 보행거리와 구분한다. 보행 경로 API로 실제 보행거리를 얻은 구간은 '실측'으로 표시한다.
- 실제 보행거리를 얻지 못한 도보 구간은 **추정 보행거리 = 좌표 간 직선거리 × 1.3**으로 계산하고 '추정'으로 표시한다. 도보 시간은 **보행 속도 4km/h**로 계산해 분 단위로 올린다(500m ≈ 8분).
- 추정 보행거리로 최대 도보 거리를 판단한 구간은 '추정 충족' 또는 '추정 초과'로 표시하며, '확정 충족'으로 표시하지 않는다. 추정 구간이 하나라도 있는 코스는 이동시간 합계에 '추정 포함'을 표시한다.
- 택시 구간 등 일부 구간의 시간이 미확인이라면(도보 구간은 추정치가 있으므로 미확인이 아니다), 알려진 시간의 합은 '확인된 구간 이동시간 합계'로 표시한다.
- 미확인 시간을 0분으로 처리하지 않으며, 해당 코스를 '총 이동시간 상한 충족'으로 표시하지 않는다.
- 대중교통 API의 소요시간에 접근 도보·환승·대기가 포함되는지 확인하여 누락과 중복 합산을 방지한다.

### 장소 하나 교체하기

- 먼저 완성된 코스를 제공하고, 사용자는 그중 한 장소를 선택해 교체 후보를 요청한다.
- 예: 카페 A를 바꾸려면 **카페 A 기준 반경 100m 내 같은 카테고리의 다른 장소**를 추천한다.
- 100m 반경은 좌표 기준 직선거리로 해석한다. 실제 보행거리 제한과는 별도로 적용한다.
- 원래 장소와 코스에 이미 포함된 장소는 교체 후보에서 제외한다. 선택 지역과 기존 취향·날씨 조건도 유지한다.
- 교체 후보를 고르면 해당 장소만 바꾸고 나머지 장소와 방문 순서는 유지한다.
- 교체 장소의 앞뒤 이동 구간을 다시 조회하여 구간별 도보 거리와 총 이동시간 상한을 재검증한다. 조건을 벗어나면 조건에 맞는 교체로 확정하지 않고, 결과에 어떤 조건을 벗어났는지 표시한다.
- **반경 100m 내 후보가 없으면 '반경 100m 내 교체 후보 없음'을 안내하고, 사용자가 선택할 때만 반경을 300m, 500m 순으로 넓힌다.** 시스템이 자동으로 반경을 넓히지 않는다. 확대한 반경은 결과에 표시하며, 500m에서도 후보가 없으면 '교체 후보 없음'으로 종료한다. 확대한 반경의 후보도 선택 지역 안에 있어야 한다.

## 4. 추천 결과

- 방문 장소와 방문 순서
- 각 장소의 카테고리, 실내·실외 구분, 날씨 반영 여부, 분위기 태그(또는 미확인), 예상 도착 시각과 운영시간(또는 미확인), '취향 외' 여부
- 구간별 이동수단, 노선·환승 정보, 거리와 예상 이동시간(실측·추정 구분), 접근·환승 도보 거리
- 총 예상 이동시간 또는 확인된 구간 이동시간 합계
- 도보 거리·이동시간 조건의 충족 여부와 판단할 수 없는 구간
- 경로 없음, 경로 조회 실패, 택시 대안 등 구간별 상태
- 날씨 판단에 사용한 기준 시각·기온·강수확률, 또는 '날씨 미반영'. 실내 우선 모드가 사용자 선호를 덮어썼으면 그 이유
- **지도: 장소 좌표에 마커를 찍고 방문 순서를 번호로 표시한다.** 구간별 경로선은 그리지 않는다. 지도 제공자는 스마트서울맵과 카카오맵 중 7장 검증 후 하나를 선택한다.

## 5. 사용자 워크플로우와 화면

### 5.1 원칙

- **모바일 우선 반응형**으로 설계하고 데스크톱에서도 동작한다.
- 언어는 **한국어만** 제공한다.
- 로그인 없이 모든 기능을 사용한다. 로그인·계정 기능은 두지 않는다.
- 코스 저장·공유·최근 기록 기능은 이번 범위에 포함하지 않는다.
- 관리자 화면은 두지 않는다. 지역·행정동 목록, 실내외 분류 보완, 분위기 키워드 규칙은 저장소 내 설정 파일과 수집 스크립트로 관리한다.
- 동네 설정 파일은 동네마다 자치구, 동네 식별자, 표시명, 소속 행정동 코드 목록, 대표 좌표를 가진다. 대표 좌표는 행정동 경계의 중심으로 계산해 채운다. 설정 파일 검증 스크립트는 서울의 모든 행정동 코드가 정확히 한 동네에 속하는지 확인하며, 빠지거나 겹치면 빌드를 실패시킨다.

### 5.2 페이지 구성

| 페이지 | 경로 | 역할 |
| --- | --- | --- |
| 홈 | `/` | 코스 요청 입력 폼. 지역(자치구 → 동네 선택 또는 검색), 방문 날짜·시간, 코스 구성, 총 이동시간 상한, 허용 교통수단, 최대 도보 거리를 입력하고 '추천받기'를 누른다. |
| 결과 | `/result` | 추천 코스를 지도와 타임라인으로 표시한다. 장소 상세, 장소 교체, 다시 추천을 여기서 수행한다. |
| 설정 | `/settings` | 취향 항목 수정, 취향 초기화. |

- 온보딩(취향 설정)은 별도 페이지가 아니라 모달로 제공한다.
- 하단 네비게이션은 두지 않는다. 헤더의 설정 아이콘으로 설정 페이지에 진입한다.
- 홈의 입력값은 기기에 저장해 다음 방문 시 마지막 값을 기본값으로 채운다.

### 5.3 첫 방문 흐름

1. 최초 접속 시 취향 설정 온보딩을 모달로 띄운다. 음식 종류, 놀거리 유형, 분위기, 실내·실외 선호를 순서대로 고른다.
2. 온보딩은 '건너뛰기'로 닫을 수 있다. 건너뛴 사용자는 취향 없이 전체 허용으로 추천받으며, 설정 페이지에서 나중에 입력할 수 있다.
3. 온보딩 완료 또는 건너뛰기 후 홈으로 이동한다. 온보딩은 기기당 한 번만 자동으로 띄운다.

### 5.4 코스 요청과 결과 흐름

1. 홈에서 조건을 입력하고 '추천받기'를 누른다. 입력 검증(2장)에 실패하면 해당 필드에 오류를 표시하고 요청하지 않는다.
2. 결과 페이지로 이동하며 **단계별 진행 상태**를 표시한다. 단계는 '장소 찾는 중 → 날씨 확인 중 → 경로 계산 중' 순이다.
3. 한 번의 요청에 **코스 1개**를 보여준다.
4. 결과 화면은 **상단 지도 + 하단 타임라인**이다. 지도에는 장소 마커와 순서 번호를 표시하고, 타임라인은 장소 → 이동 구간 → 장소 순으로 세로 배치한다. 각 이동 구간에는 이동수단, 거리, 시간, 구간 상태(4장)를 표시한다.
5. 타임라인의 장소를 누르면 **바텀시트(데스크톱은 사이드 패널)**로 상세를 연다. 상세에는 장소명, 주소, 운영시간, 설명, 실내·실외 구분, 분위기 태그, 예상 도착 시각과 '이 장소 교체' 버튼을 둔다.
6. '다시 추천' 버튼을 누르면 같은 조건으로 새 코스를 계산한다. 이번 결과 세션에서 이미 보여준 장소는 후보에서 제외한다. 제외로 후보가 부족해지면 '더 이상 새로운 장소가 없습니다'를 안내하고 제외 목록을 초기화할지 묻는다.
7. 홈으로 돌아가 조건을 바꾸면 제외 목록은 초기화된다.

### 5.5 장소 교체 흐름

1. 장소 상세의 '이 장소 교체'를 누르면 바텀시트가 교체 후보 목록으로 전환된다. 후보는 3장 '장소 하나 교체하기' 규칙을 따른다.
2. 후보가 없으면 '반경 100m 내 교체 후보 없음'과 함께 '300m로 넓히기' 버튼을 보여준다. 300m에도 없으면 '500m로 넓히기'를 보여주고, 500m에도 없으면 '교체 후보 없음'으로 종료한다.
3. 후보를 고르면 앞뒤 구간을 다시 조회하는 동안 해당 구간에 로딩을 표시한다. 재검증 결과 조건을 벗어나면 교체를 적용하되 벗어난 조건을 구간 상태에 표시한다.

### 5.6 로딩과 오류 처리

- 장소 후보 조회 자체가 실패하면 결과 페이지에 전체 오류 화면과 '다시 시도' 버튼을 표시한다.
- 날씨 조회 실패는 전체 오류가 아니다. '날씨 미반영'으로 표시하고 코스는 정상 제공한다.
- 일부 이동 구간의 경로 조회가 실패하면 코스는 보여주되 해당 구간에 '경로 조회 실패'와 구간별 '재시도' 버튼을 표시한다. 재시도는 그 구간만 다시 조회한다.
- 취향 필터는 3장 '취향 완화' 규칙으로 자동 완화되므로 취향 때문에 코스 생성이 실패하지 않는다. 이동·도보·운영시간 조건에 맞는 코스가 없으면 '조건에 맞는 코스가 없습니다'와 함께 조건 완화 안내(도보 거리 늘리기, 이동시간 늘리기, 교통수단 추가)를 표시하고 홈으로 돌아가는 버튼을 둔다.

### 5.7 설정 페이지

- 취향 네 항목을 온보딩과 같은 UI로 수정한다. 저장 즉시 반영한다.
- '취향 초기화' 버튼을 두고, 누르면 확인 후 기기에 저장된 취향과 온보딩 완료 표시를 삭제한다. 삭제 후 홈으로 이동하면 온보딩을 다시 띄운다.
- 취향이 기기에만 저장되며 다른 기기와 공유되지 않는다는 안내 문구를 표시한다.

### 5.8 디자인 시스템 적용

- 랜딩페이지와 MVP 세 페이지의 시각 언어는 저장소 루트의 `DESIGN.md`를 따르며, 구현은 `design/tokens.css`의 CSS 커스텀 프로퍼티를 사용한다. 두 파일은 함께 수정한다.
- **랜딩 영역**은 별도 경로가 아니라 홈 `/`의 입력 폼 **위**에 둔다. 첫 방문(온보딩 미완료) 사용자에게는 히어로부터 보이고, 재방문 사용자는 헤더의 '추천받기' 앵커로 폼까지 바로 스크롤한다. 랜딩 섹션 순서: 히어로 → 3열 피처 카드 → 지역 쇼케이스 → 4열 안심 타일 → FAQ → 검정 프로모 스트립 → 푸터.
- **CTA 색 규칙**: 랜딩(마케팅 표면)의 주 CTA는 검정 필 `button-primary`, 보조는 아웃라인 `button-secondary`다. 코스 요청 폼·결과 페이지·바텀시트(커머스 표면)의 주 CTA('추천받기', '다시 추천', '이 장소 교체', '300m로 넓히기')만 코발트 필 `button-buy-cta`를 쓴다. 코발트는 이 세 표면 밖에 나오지 않는다.
- **서체**: Optimistic VF는 라이선스가 없어 포함하지 않는다. 배포 서체는 Pretendard Variable이며 폴백은 Montserrat → Helvetica → Arial → Noto Sans KR이다. 모든 heading은 `font-feature-settings: "ss01", "ss02"`를 켠다. 본문 16px/1.50/-0.16px, 보조 14px/1.43/-0.14px.
- **이미지**: 히어로·지역 쇼케이스 사진은 32px 라운드(`{rounded.xxxl}`) 프레임에 테두리·그림자 없이 넣는다. 히어로는 16:9 이상, 지역 쇼케이스는 4:3, 지역 선택 타일과 장소 썸네일은 1:1에 8px/16px 라운드다. 장소 사진이 없으면 연회색(`{colors.surface-soft}`) 배경 + 카테고리 라인 아이콘으로 대체한다. 사진 위 흰 글자에는 `rgba(10,19,23,0.12)` 오버레이를 깐다. 이미지 파일은 저작권을 확인한 뒤 `public/images/`에 두고 `next/image`로 제공한다.

| 화면 요소 | DESIGN.md 컴포넌트 | 비고 |
| --- | --- | --- |
| 헤더 | Top Navigation + `button-icon-circular`(설정) | 하단 내비 없음(5.2). 모바일은 워드마크 + 설정 아이콘만 |
| 날씨 실내 우선 안내 | `promo-banner-yellow` | 실내 우선 모드일 때만 내비 위에 표시 |
| 랜딩 히어로 | `hero-band-marketing` | `hero-display` 64px → 36px(<768) → 24px(<480) |
| 랜딩 피처 카드 | `card-product-feature` ×3 | 날씨 반영 · 이동시간 상한 · 장소 교체 |
| 지역 쇼케이스 | `card-feature-photo` | 우선 검수 지역 5곳(성수·서촌·익선·홍대·연남)을 예시로 노출, 카피 좌하단 흰색. '서울 전역 25개 구' 문구 포함 |
| 안심 타일 | `feature-icon-row` (`card-icon-feature` ×4) | 로그인 없음 · 기기 저장 · 공공 데이터 · 택시 없는 코스 우선 |
| FAQ | `faq-accordion` | |
| 홈 입력 폼 | `course-request-panel` (`card-checkout-summary` 밀도) | 섹션 제목 `subtitle-lg`, 그룹 간격 20px |
| 지역 선택 | `region-picker` = `text-input` 검색 + 자치구 `pill-tab` 행 + 동네 `region-picker-row`(`color-sku-picker-row`) | 자치구를 고르면 그 구의 동네 타일만 표시. 1:1 타일, 선택 시 2px `ink-deep` 테두리, 장소 데이터 부족 동네는 `-disabled` + `badge-attention` |
| 방문 날짜·시간, 총 이동시간, 최대 도보 거리 | `text-input` / `-focused` / `-error` | 44px, 오류는 `critical-strong` |
| 코스 구성(카페·식당·놀거리 개수) | `pill-tab` 스텝퍼 + `badge-neutral` | 최소 2·최대 5 초과 시 `-disabled` |
| 허용 교통수단 | 체크박스(`fb-blue`) | 택시 기본 꺼짐 |
| 실내·실외 선호 | `radio-option` / `-selected` | 선택 시 2px `primary-deep` |
| '추천받기' | `button-buy-cta` 전폭 | |
| 결과: 지도 | `result-gallery` 좌측, `rounded-xxxl` | 마커 = 32px 원형 순서 번호, 경로선 없음 |
| 결과: 요약 레일 | `card-checkout-summary` (sticky, max 380px) | <1024px 하단 고정 바(총 이동시간 + '다시 추천') |
| 결과: 타임라인 | `course-timeline` | 장소 카드 `rounded-xl` → 이동 구간 행 → 장소 카드 |
| 구간 상태 | `badge-success`(확정 충족·실측) · `badge-attention`(추정·택시 이용 검토) · `badge-critical`(경로 없음·조회 실패) + `button-ghost` 재시도 | |
| 장소 상태 | `badge-attention`(취향 외·운영시간 미확인·분위기 미확인) · `badge-neutral`(카테고리·실내/실외) | |
| 날씨 판단 근거 | `tech-specs-table` + `primary-soft` 콜아웃 | 기준 시각·기온·강수확률, 선호 덮어쓴 이유 |
| 장소 상세 | `place-bottom-sheet` (데스크톱 사이드 패널) | 상단 `rounded-xxl`, 그림자 Level 2, `button-buy-cta` '이 장소 교체' |
| 교체 후보·반경 확대 | `radio-option` 목록 + `button-buy-cta` '300m로 넓히기' | 500m에도 없으면 `badge-critical` '교체 후보 없음' |
| 단계별 로딩 | `badge-neutral` 3단계 스텝 + 스켈레톤(`surface-soft`) | 장소 찾는 중 → 날씨 확인 중 → 경로 계산 중 |
| 전체 오류·조건 없음 | `card-product-feature` 안내 + `button-buy-cta` '다시 시도' / `button-ghost` '홈으로' | |
| 온보딩 모달 | `card-checkout-summary` 크롬, 취향 칩 `pill-tab`, '건너뛰기' `button-ghost`, '완료' `button-buy-cta` | |
| 설정 페이지 | 온보딩과 같은 UI + `warranty-card` 기기 저장 안내 + '취향 초기화' `button-secondary` | 초기화 확인은 `badge-critical` 톤 문구 |
| 푸터 | `footer-region` | 데이터 출처 표기 `caption` `stone` |

- 디자인 미리보기는 `design/preview.html`에서 확인한다. 토큰을 바꾸면 미리보기도 함께 갱신한다.

## 6. 기술 스택

- **Next.js(App Router) + TypeScript** 단일 저장소로 프론트엔드와 백엔드를 함께 구성한다.
- 공공 API 호출은 **서버 라우트(Route Handler)에서만** 수행한다. API 키는 서버 환경변수에만 두고 브라우저에 노출하지 않는다.
- 인증·계정 시스템은 두지 않는다. 취향과 홈 입력값은 브라우저 localStorage에만 저장한다.
- 서버 DB는 장소 데이터 캐시 용도로만 사용한다. 사용자별 데이터를 서버에 저장하지 않는다.
- 장소 데이터는 공공 API를 주기적으로 수집해 서버 DB에 캐시한다. 행정동 소속, 실내·실외 분류, 분위기 태그는 캐시된 데이터에 대한 후처리로 붙인다.

## 7. API 구성

| API 또는 데이터 | 역할 | 채택 전 확인할 사항 |
| --- | --- | --- |
| Visit Seoul 기반 서울시 관광 문화 데이터 | 방문 후보의 장소명·주소·이용시간·설명문 등 | 사용할 실제 API, 좌표·운영정보 누락 여부, 운영시간 필드의 형식(요일별·휴무일 표현), 실내·실외 분류 제공 여부, 음식 종류·놀거리 유형과 대응되는 분류 필드 유무 |
| 서울시 실시간 도시데이터 | 제공 대상 장소의 날씨 등 보조 정보 | 주요 121개 장소의 제공 범위와 추천 장소의 대응 관계 |
| 기상청_단기예보 조회서비스 | 방문 지역·시간대의 날씨 예보(TMP·POP) | 지역 대표 좌표의 예보 격자 변환, 발표 시각과 예보 가능 기간 |
| 서울특별시_대중교통환승경로 조회 서비스 | 버스 및 버스·지하철 환승 경로 | 실제 응답, 소요시간·도보 구간 필드, 수단별 조회 범위 |
| 서울교통공사_최단경로이동정보 | 지하철 역 간 경로와 이동시간 | 지원 역·노선, 역 식별자, 시간표 및 소요시간의 의미 |
| 스마트서울맵 OpenAPI 또는 카카오맵 | 주소·좌표 변환, 행정동 판정, 지도 마커 표시 | 스마트서울맵 V5 공식 목차에는 일반 POI 검색이 명시되지 않으므로 장소 검색 제공을 전제하지 않는다. 지도 표시·행정동 역지오코딩 가능 여부를 두 제공자 모두 검증한 뒤 하나를 채택한다. |
| 행정동 경계 데이터 | 장소의 지역 소속 판정 | 서울 열린데이터 또는 통계청 행정동 경계 파일의 좌표계와 갱신 주기 |

- 현재 목록만으로 장소 간 전체 도보 경로와 실제 보행거리를 제공할 수 있는지는 미확인이다. 제공되지 않으면 3장의 추정 보행거리(직선거리 × 1.3)로 동작하며, 실측 제공은 이후 개선 항목으로 둔다.
- 현재 목록에 택시 경로·시간 계산 API는 포함하지 않는다.
- 100m 내 같은 카테고리의 대체 장소를 찾으려면 장소별 좌표와 카테고리가 필요하다. 공공 관광 데이터가 해당 지역의 카페·식당·놀거리를 얼마나 포함하는지 실제 데이터로 확인한다. 후보가 항상 존재한다고 보장하지 않는다. 특히 주거 위주 동네는 관광 데이터의 장소 수가 적을 수 있으므로, 수집 후 동네별 카테고리 후보 수를 집계해 2.3의 '장소 데이터 부족' 표시에 반영한다.
- 각 API의 실제 사용 가능 여부, 인증 방식, 호출 한도 및 출처 표시 조건을 확인한다.

공식 참고 문서:

- [서울시 관광 문화](https://data.seoul.go.kr/dataList/OA-21052/S/1/datasetView.do)
- [서울시 실시간 도시데이터](https://data.seoul.go.kr/dataList/OA-21285/A/1/datasetView.do)
- [기상청_단기예보 조회서비스](https://www.data.go.kr/data/15084084/openapi.do)
- [서울특별시_대중교통환승경로 조회 서비스](https://www.data.go.kr/data/15000414/openapi.do)
- [서울교통공사_최단경로이동정보](https://www.data.go.kr/data/15143842/openapi.do)
- [스마트서울맵 OpenAPI](https://map.seoul.go.kr/smgis2/openApi)
- [스마트서울맵 V5 연동 안내](https://map.seoul.go.kr/smgis2/division/viewOpenApi)

## 8. 완료 판단 기준

- 사용자에게 보여주는 추천 코스의 방문 장소가 선택 지역의 행정동 안에 속한다.
- 서울 25개 자치구의 모든 행정동이 정확히 하나의 동네에 속하며, 홈에서 모든 자치구의 동네를 선택할 수 있다. 설정 파일 검증 스크립트가 누락·중복 없음을 확인한다.
- 카테고리 후보가 없는 동네는 선택할 수 없고 부족한 카테고리가 안내된다.
- 처음 설정한 취향을 이후 지역별 추천에도 반영한다. 브라우저를 닫았다 다시 열어도 취향이 유지된다.
- 취향 초기화 후 다음 접속 시 온보딩이 다시 표시된다.
- 취향이나 사용자 식별 정보가 서버로 전송되지 않는다.
- 코스 구성이 요청한 카테고리별 개수와 일치하고, 총 장소 수가 2곳 이상 5곳 이하이다.
- 택시 없는 코스를 우선 추천한다. 택시가 꺼진 상태에서는 '택시 이용 검토' 표시가 나타나지 않는다.
- 장소 교체 후보는 기존 장소를 제외한 같은 카테고리이며, 기존 장소 기준 반경 100m 이내여야 한다. 반경 확대는 사용자가 선택했을 때만 300m·500m 순으로 일어난다.
- 장소 교체 후에도 앞뒤 이동 구간과 전체 이동시간 조건을 다시 확인한다.
- 허용하지 않은 승차 수단을 이용 가능한 경로로 추천하지 않는다. 접근·환승 도보는 도보 미선택 시에도 허용된다.
- 보행거리(실측 또는 추정)가 설정값과 같으면 도보 후보에 포함하고, 초과하면 제외한다. 추정 구간은 '추정' 표시가 붙고 '확정 충족'으로 표시되지 않는다.
- 기온 30°C 이상, 0°C 이하, 강수확률 60% 이상 중 하나라도 해당하고 실내 분류가 확인된 경우에만 날씨를 반영한 실내 추천으로 표시한다.
- 실외 선호 사용자에게 실내 우선 모드가 적용되면 실내 장소가 추천되고 그 이유가 표시된다.
- 취향 필터 통과 후보가 부족한 카테고리는 같은 카테고리 다른 장소로 채워지고 '취향 외'가 표시된다. 이동시간·도보·운영시간 조건은 자동 완화되지 않는다.
- 경로 없음과 API 조회 실패를 구분한다.
- 일부 구간의 거리·시간이 미확인인 코스를 모든 조건이 충족된 코스로 표시하지 않는다.
- 분위기 태그가 없는 장소가 후보에서 배제되지 않는다.
- 예상 도착 시각에 영업 중이 아닌 장소가 코스에 포함되지 않는다. 운영시간 미확인 장소는 포함되며 '운영시간 미확인'이 표시된다.
- 온보딩을 건너뛴 사용자도 코스를 추천받을 수 있다.
- '다시 추천'을 반복하면 이미 보여준 장소가 다시 나오지 않으며, 후보 소진 시 안내가 표시된다.
- 일부 구간의 경로 조회 실패가 전체 결과를 막지 않고, 해당 구간만 재시도할 수 있다.
- 모바일 화면 폭(360px)에서 홈·결과·설정 페이지가 가로 스크롤 없이 동작한다.
- 공공 API 키가 브라우저 번들이나 네트워크 응답에 노출되지 않는다.
- 실제 API 호출 결과로 장소 조회와 구간별 경로·시간 연결을 검증한 후, 검증한 교통수단 범위만 구현 완료로 판단한다.

## 9. 확정 이력

이전 판에서 미확정으로 남겼던 항목과 확정 내용이다.

| 항목 | 확정 내용 |
| --- | --- |
| 초기 취향 항목 | 음식 종류·놀거리 유형(필터), 분위기·실내·실외 선호(정렬 가중치) |
| 취향 저장 방식 | 디바이스 내(localStorage) 저장만. 로그인·계정 동기화 없음 (이전 결정인 '기기 우선 + 로그인 동기화'를 변경) |
| 코스 장소 수와 구성 | 기본 카페 1·식당 1·놀거리 1, 사용자가 추가·제거. 최소 2곳·최대 5곳 |
| 추천 지역 경계 | 행정동 단위. 서울 25개 자치구 전역의 동네를 제공(부록 A). 우선 검수 지역은 성수·서촌(통의)·익선·홍대·연남 5곳 (이전 결정인 '초기 5곳만 제공'을 변경) |
| 출발 위치·방문 시각 | 출발 위치 없음. 날짜·시간만 입력, 기본값 현재 시각 |
| 구간별 최대 도보 거리 기본값 | 500m |
| 총 이동시간 상한 기본값 | 60분 |
| 덥다·춥다·비 기준 | 기온 30°C 이상 / 0°C 이하 / 강수확률 60% 이상 |
| 접근·환승 도보 | 거리 제한 미적용, 도보 미선택 시에도 허용 |
| 100m 내 교체 후보 없음 | 안내 후 사용자 선택으로 300m·500m 확대. 자동 확대 없음 |
| 택시 옵션 | 교통수단 체크박스에 포함, 기본 꺼짐, 대안 표시 전용 |
| 분위기 태그 생성 | 설명문 키워드 매칭. 미태깅 장소는 배제하지 않음 |
| 기술 스택 | Next.js(App Router) + TypeScript, 서버 라우트에서 공공 API 호출 |
| 지도 표시 | 장소 마커와 순서 번호. 경로선 없음 |
| 첫 방문 흐름 | 취향 온보딩 모달 제시, 건너뛰기 허용 |
| 결과 개수 | 코스 1개 + '다시 추천'(이미 보여준 장소 제외) |
| 결과 레이아웃 | 상단 지도 + 하단 타임라인, 장소 상세는 바텀시트 |
| 저장·공유·기록 | 이번 범위 제외 |
| 운영시간 | 예상 도착 시각(체류 카페 60·식당 60·놀거리 90분) 기준 영업 중인 장소만 후보 |
| 기기·언어 | 모바일 우선 반응형, 한국어만 |
| 페이지 구성 | 홈(입력 폼) / 결과 / 설정 3개 |
| 로딩·오류 | 단계별 진행 표시, 부분 실패는 구간에 표시하고 구간별 재시도 |
| 로그인 진입점 | 없음. 로그인 기능 자체를 제거 (이전 결정인 '설정 페이지에만'을 변경) |
| 관리자 화면 | 없음. 설정 파일·스크립트로 관리 |
| 날씨와 실내·실외 선호 충돌 | 날씨(실내 우선 모드)가 이기고 이유를 표시 |
| 실내·실외 분류 보완 | 카테고리 기반 자동 추정 후 우선 검수 지역 장소는 수동 보정 |
| 실제 보행거리 부재 시 | 직선거리 × 1.3 추정, 보행 속도 4km/h 분 단위 올림, '추정' 표시 |
| 취향 후보 부족 시 | 해당 카테고리 취향 필터만 자동 완화, '취향 외' 표시. 이동·도보·운영시간 조건은 완화 없음 |
| 디자인 시스템 | Meta 커머스 표면 기반 `DESIGN.md` + `design/tokens.css`. 랜딩은 홈 `/` 상단, 마케팅 CTA 검정 필·커머스 CTA 코발트 필 |
| 서체 | Pretendard Variable(Optimistic VF 대체), heading에 ss01·ss02, 본문 16px/1.50/-0.16px |
| 이미지 규격 | 히어로 16:9 이상·지역 쇼케이스 4:3 32px 라운드, 타일·썸네일 1:1 8–16px 라운드, 사진 없으면 연회색 + 아이콘 |

## 부록 A. 서울 전역 동네 목록

홈에서 선택할 수 있는 동네와 소속 행정동이다. 서울 25개 자치구의 모든 행정동을 빠짐없이, 겹침 없이 배정했다. 총 147개 동네, 426개 행정동이다. 행정동 명칭은 2023년 이후 기준(강남구 일원2동의 개포3동 개칭 반영)이며, 데이터 수집 시 행정안전부 최신 행정동 코드와 대조해 확정한다. 같은 이름의 행정동이 다른 자치구에 있을 수 있으므로(강남구·관악구의 신사동 등) 설정 파일은 이름이 아니라 행정동 코드로 배정한다. 동네 이름은 생활권 이름을 우선하되, 검색은 소속 행정동명으로도 가능하므로 표시명에 모든 행정동명을 넣지 않는다.

### 종로구

| 동네 | 소속 행정동 |
| --- | --- |
| 서촌(통의) | 청운효자동·사직동 |
| 북촌·삼청 | 가회동·삼청동 |
| 익선·인사동 | 종로1·2·3·4가동 |
| 동대문시장·종로5가 | 종로5·6가동 |
| 대학로·혜화 | 혜화동·이화동 |
| 부암·평창 | 부암동·평창동 |
| 독립문·교남 | 무악동·교남동 |
| 창신·숭인 | 창신1동·창신2동·창신3동·숭인1동·숭인2동 |

### 중구

| 동네 | 소속 행정동 |
| --- | --- |
| 명동·소공 | 명동·소공동 |
| 남대문·회현 | 회현동 |
| 을지로 | 을지로동 |
| 충무로·필동 | 필동 |
| 장충·DDP | 장충동·광희동 |
| 신당·황학 | 신당동·신당5동·황학동·동화동 |
| 약수·청구 | 약수동·청구동·다산동 |
| 서울역·중림 | 중림동 |

### 용산구

| 동네 | 소속 행정동 |
| --- | --- |
| 이태원·경리단길 | 이태원1동·이태원2동 |
| 한남 | 한남동 |
| 해방촌·후암 | 후암동·용산2가동 |
| 용리단길·삼각지 | 한강로동 |
| 남영·숙대입구 | 남영동·청파동 |
| 효창·원효로 | 효창동·용문동·원효로1동·원효로2동 |
| 이촌 | 이촌1동·이촌2동 |
| 서빙고·보광 | 서빙고동·보광동 |

### 성동구

| 동네 | 소속 행정동 |
| --- | --- |
| 성수·서울숲 | 성수1가1동·성수1가2동·성수2가1동·성수2가3동 |
| 왕십리·행당 | 왕십리도선동·왕십리2동·행당1동·행당2동 |
| 마장·사근 | 마장동·사근동 |
| 금호·옥수 | 금호1가동·금호2·3가동·금호4가동·옥수동 |
| 응봉 | 응봉동 |
| 송정·용답 | 송정동·용답동 |

### 광진구

| 동네 | 소속 행정동 |
| --- | --- |
| 건대입구 | 화양동·자양4동 |
| 어린이대공원·군자 | 군자동·능동 |
| 중곡 | 중곡1동·중곡2동·중곡3동·중곡4동 |
| 구의 | 구의1동·구의2동·구의3동 |
| 광장 | 광장동 |
| 자양 | 자양1동·자양2동·자양3동 |

### 동대문구

| 동네 | 소속 행정동 |
| --- | --- |
| 청량리·제기 | 청량리동·제기동·용신동 |
| 회기·휘경 | 회기동·휘경1동·휘경2동 |
| 이문 | 이문1동·이문2동 |
| 전농·답십리 | 전농1동·전농2동·답십리1동·답십리2동 |
| 장안 | 장안1동·장안2동 |

### 중랑구

| 동네 | 소속 행정동 |
| --- | --- |
| 면목 | 면목본동·면목2동·면목3·8동·면목4동·면목5동·면목7동 |
| 상봉·망우 | 상봉1동·상봉2동·망우본동·망우3동 |
| 중화 | 중화1동·중화2동 |
| 먹골·묵동 | 묵1동·묵2동 |
| 신내 | 신내1동·신내2동 |

### 성북구

| 동네 | 소속 행정동 |
| --- | --- |
| 성북동 | 성북동 |
| 성신여대·돈암 | 동선동·돈암1동·돈암2동·삼선동 |
| 안암·보문 | 안암동·보문동 |
| 정릉 | 정릉1동·정릉2동·정릉3동·정릉4동 |
| 길음 | 길음1동·길음2동 |
| 종암·월곡 | 종암동·월곡1동·월곡2동 |
| 장위·석관 | 장위1동·장위2동·장위3동·석관동 |

### 강북구

| 동네 | 소속 행정동 |
| --- | --- |
| 미아·삼양 | 미아동·삼양동·송중동·송천동·삼각산동 |
| 번동 | 번1동·번2동·번3동 |
| 수유 | 수유1동·수유2동·수유3동 |
| 우이·인수 | 우이동·인수동 |

### 도봉구

| 동네 | 소속 행정동 |
| --- | --- |
| 쌍문 | 쌍문1동·쌍문2동·쌍문3동·쌍문4동 |
| 방학 | 방학1동·방학2동·방학3동 |
| 창동 | 창1동·창2동·창3동·창4동·창5동 |
| 도봉 | 도봉1동·도봉2동 |

### 노원구

| 동네 | 소속 행정동 |
| --- | --- |
| 노원역 | 상계2동·상계6·7동·상계10동 |
| 상계·수락산 | 상계1동·상계3·4동·상계5동·상계8동·상계9동 |
| 중계 | 중계본동·중계1동·중계2·3동·중계4동 |
| 하계 | 하계1동·하계2동 |
| 공릉·태릉 | 공릉1동·공릉2동 |
| 월계 | 월계1동·월계2동·월계3동 |

### 은평구

| 동네 | 소속 행정동 |
| --- | --- |
| 연신내·불광 | 불광1동·불광2동·대조동·갈현1동·갈현2동 |
| 녹번·응암 | 녹번동·응암1동·응암2동·응암3동 |
| 구산·역촌 | 구산동·역촌동 |
| 신사(은평) | 신사1동·신사2동 |
| 수색·증산 | 수색동·증산동 |
| 은평뉴타운·진관 | 진관동 |

### 서대문구

| 동네 | 소속 행정동 |
| --- | --- |
| 신촌·이대 | 신촌동 |
| 연희 | 연희동 |
| 충정로·북아현 | 천연동·북아현동·충현동 |
| 홍제 | 홍제1동·홍제2동·홍제3동 |
| 홍은 | 홍은1동·홍은2동 |
| 가좌 | 남가좌1동·남가좌2동·북가좌1동·북가좌2동 |

### 마포구

| 동네 | 소속 행정동 |
| --- | --- |
| 홍대 | 서교동 |
| 연남 | 연남동 |
| 합정·상수 | 합정동·서강동 |
| 망원 | 망원1동·망원2동 |
| 공덕·마포 | 공덕동·도화동·용강동 |
| 아현·대흥 | 아현동·대흥동·염리동·신수동 |
| 성산 | 성산1동·성산2동 |
| 상암 | 상암동 |

### 양천구

| 동네 | 소속 행정동 |
| --- | --- |
| 목동 | 목1동·목2동·목3동·목4동·목5동 |
| 신정 | 신정1동·신정2동·신정3동·신정4동·신정6동·신정7동 |
| 신월 | 신월1동·신월2동·신월3동·신월4동·신월5동·신월6동·신월7동 |

### 강서구

| 동네 | 소속 행정동 |
| --- | --- |
| 염창·등촌 | 염창동·등촌1동·등촌2동·등촌3동 |
| 화곡 | 화곡본동·화곡1동·화곡2동·화곡3동·화곡4동·화곡6동·화곡8동·우장산동 |
| 가양 | 가양1동·가양2동·가양3동 |
| 마곡·발산 | 발산1동 |
| 공항·방화 | 공항동·방화1동·방화2동·방화3동 |

### 구로구

| 동네 | 소속 행정동 |
| --- | --- |
| 신도림 | 신도림동 |
| 구로디지털·구로 | 구로1동·구로2동·구로3동·구로4동·구로5동·가리봉동 |
| 고척 | 고척1동·고척2동 |
| 개봉 | 개봉1동·개봉2동·개봉3동 |
| 오류·항동 | 오류1동·오류2동·수궁동·항동 |

### 금천구

| 동네 | 소속 행정동 |
| --- | --- |
| 가산디지털 | 가산동 |
| 독산 | 독산1동·독산2동·독산3동·독산4동 |
| 시흥(금천) | 시흥1동·시흥2동·시흥3동·시흥4동·시흥5동 |

### 영등포구

| 동네 | 소속 행정동 |
| --- | --- |
| 여의도 | 여의동 |
| 영등포역·타임스퀘어 | 영등포본동·영등포동 |
| 문래·도림 | 문래동·도림동 |
| 당산·양평 | 당산1동·당산2동·양평1동·양평2동 |
| 신길 | 신길1동·신길3동·신길4동·신길5동·신길6동·신길7동 |
| 대림 | 대림1동·대림2동·대림3동 |

### 동작구

| 동네 | 소속 행정동 |
| --- | --- |
| 노량진 | 노량진1동·노량진2동 |
| 상도·숭실대 | 상도1동·상도2동·상도3동·상도4동 |
| 흑석·중앙대 | 흑석동 |
| 사당·이수 | 사당1동·사당2동·사당3동·사당4동·사당5동 |
| 보라매·대방 | 대방동·신대방1동·신대방2동 |

### 관악구

| 동네 | 소속 행정동 |
| --- | --- |
| 서울대입구·샤로수길 | 청룡동·행운동·중앙동·은천동·성현동·청림동 |
| 낙성대·남현 | 낙성대동·인헌동·남현동 |
| 신림역 | 신림동·서원동·신원동·서림동·신사동 |
| 대학동·난곡 | 대학동·삼성동·조원동·미성동·난향동·난곡동 |
| 보라매(관악) | 보라매동 |

### 서초구

| 동네 | 소속 행정동 |
| --- | --- |
| 강남역(서초)·교대 | 서초1동·서초2동·서초3동·서초4동 |
| 반포·잠원·서래마을 | 반포본동·반포1동·반포2동·반포3동·반포4동·잠원동 |
| 방배 | 방배본동·방배1동·방배2동·방배3동·방배4동 |
| 양재·내곡 | 양재1동·양재2동·내곡동 |

### 강남구

| 동네 | 소속 행정동 |
| --- | --- |
| 가로수길·신사 | 신사동 |
| 압구정 | 압구정동 |
| 청담 | 청담동 |
| 논현 | 논현1동·논현2동 |
| 강남역(역삼)·역삼 | 역삼1동·역삼2동 |
| 삼성·코엑스 | 삼성1동·삼성2동 |
| 대치 | 대치1동·대치2동·대치4동 |
| 도곡 | 도곡1동·도곡2동 |
| 개포 | 개포1동·개포2동·개포3동·개포4동 |
| 일원·수서 | 일원본동·일원1동·수서동 |
| 세곡 | 세곡동 |

### 송파구

| 동네 | 소속 행정동 |
| --- | --- |
| 잠실 | 잠실본동·잠실2동·잠실3동·잠실4동·잠실6동·잠실7동 |
| 송리단길·석촌 | 석촌동·송파1동·송파2동·삼전동 |
| 방이·올림픽공원 | 방이1동·방이2동·오륜동 |
| 풍납 | 풍납1동·풍납2동 |
| 가락·문정 | 가락본동·가락1동·가락2동·문정1동·문정2동 |
| 오금·거여·마천 | 오금동·거여1동·거여2동·마천1동·마천2동 |
| 장지·위례 | 장지동·위례동 |

### 강동구

| 동네 | 소속 행정동 |
| --- | --- |
| 천호 | 천호1동·천호2동·천호3동 |
| 성내 | 성내1동·성내2동·성내3동 |
| 길동·둔촌 | 길동·둔촌1동·둔촌2동 |
| 암사 | 암사1동·암사2동·암사3동 |
| 명일·고덕 | 명일1동·명일2동·고덕1동·고덕2동 |
| 상일·강일 | 상일1동·상일2동·강일동 |
