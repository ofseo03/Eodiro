# Copy to .env.local. REST API keys are server only.
# Optional: without this key, collect uses the same official public JSON downloads.
SEOUL_API_KEY=
# Approvals required for KMA forecasts and Seoul transit.
DATA_GO_KR_KEY=
PLACE_DB_PATH=data/places.sqlite
# Kakao Local API: REST API key, not the JavaScript key. Server only.
KAKAO_REST_API_KEY=
# Browser-visible JavaScript key for map display. Register the frontend domain in Kakao.
KAKAO_JAVASCRIPT_KEY=
